<h1>Traveler Reviews</h1>

<ol>
    <% @reviews.each do |review| %>
       <li><% link_to review.title, review_path(review) %> || <%= review.likes.count %></li>
    <% end %>
</ol>

<%= link_to "New", new_review_path %>